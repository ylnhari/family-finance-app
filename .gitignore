# Claude personal/machine-local memory (template .example is committed)
CLAUDE.local.md

# Personal financial data - NEVER commit this
data/

# Throwaway demo sandbox (server --demo seeds this from samples/) - never commit
demo-data/

# Claude Code local config (permissions, preview config — machine-specific)
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
*.pyc
__pycache__/
.vscode/
.idea/
